"""Arma 3 server integration boundary — placeholder.

A later phase adds gameplay/server integration here (telemetry ingest via the
HTTP API, RCON, persistent campaign data). Kept as a package now so the
boundary exists in the architecture from day one.
"""
